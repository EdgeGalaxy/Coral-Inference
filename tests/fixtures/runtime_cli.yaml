platform: "onnx"
patches:
  camera: true
  buffer_sink: true
  metric_sink: false
  webrtc: false
backends:
  auto_discover: false
services:
  webrtc:
    stun_servers:
      - "stun:stun.l.google.com:19302"
      - "stun:stun1.example.org:3478"
  metrics:
    influxdb_url: "http://localhost:8086"
